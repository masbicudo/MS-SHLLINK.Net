using JetBrains.Annotations;
using ShellLink.DataObjects;
using System.IO;

namespace ShellLink.Loaders
{
    public static class CommonNetworkRelativeLinkLoader
    {
        public static bool Load([NotNull] this CommonNetworkRelativeLink obj, BinaryReader reader)
        {
            var ok = true;

            return ok;
        }
    }
}