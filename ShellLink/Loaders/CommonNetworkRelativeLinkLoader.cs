using System.IO;
using ShellLink.DataObjects;

namespace ShellLink.Loaders
{
    public static class CommonNetworkRelativeLinkLoader
    {
        public static bool Load(this CommonNetworkRelativeLink obj, BinaryReader reader)
        {
            var ok = true;

            return ok;
        }
    }
}