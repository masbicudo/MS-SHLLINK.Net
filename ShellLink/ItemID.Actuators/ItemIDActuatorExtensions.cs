using ShellLink.Internals;
using System.IO;

namespace ShellLink.ItemID.Actuators
{
    public static class ItemIDActuatorExtensions
    {
        //public static byte[] GetData(this ShellItemActuator itemidActuator, IOptions options)
        //{
        //    var sz = itemidActuator.GetLength() - ShellItemId.SizeFieldLength;
        //    var buffer = new byte[sz];
        //    itemidActuator.WriteTo(
        //            new BinaryWriter(new SkipBytesMemoryStream(buffer, ShellItemId.SizeFieldLength)),
        //            options
        //        );
        //    return buffer;
        //}
    }
}